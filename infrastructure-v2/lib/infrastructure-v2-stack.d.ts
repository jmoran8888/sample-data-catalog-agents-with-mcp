import * as cdk from 'aws-cdk-lib/core';
import { Construct } from 'constructs';
export declare class InfrastructureV2Stack extends cdk.Stack {
    constructor(scope: Construct, id: string, props?: cdk.StackProps);
}
