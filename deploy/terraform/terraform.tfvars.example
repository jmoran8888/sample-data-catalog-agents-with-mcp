# Copy this file to terraform.tfvars and fill in your values
# With SSM-based access, minimal configuration is required

# AWS region (optional - defaults to us-east-1)
# aws_region = "us-east-1"

# Environment name (optional - defaults to dev)
# environment = "dev"

# Database password (optional - has default, but change for production)
# db_password = "YourSecurePassword123!"

# Note: No IP address configuration needed!
# Access is via AWS Systems Manager (SSM) port forwarding through CloudShell
# See README.md for access instructions
